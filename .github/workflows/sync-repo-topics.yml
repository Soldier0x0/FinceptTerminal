name: Sync repository topics

# Disabled on this personal fork — no hacktoberfest-excluded topic enforcement.
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "sync-repo-topics is disabled on this fork."
