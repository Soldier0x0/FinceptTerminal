name: Close stale scope-gated PRs

# Disabled on this personal fork — upstream auto-close for missing scope approval
# does not apply. See .github/CONTRIBUTING.md.
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pr-stale-close is disabled on this fork."
